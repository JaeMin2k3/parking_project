const now = new Date();
  const currentDate = now.toISOString().split('T')[0];
  const currentHour = now.getHours() + now.getMinutes() / 60;
console.log(currentDate);
console.log("ádfsdfasdf");
console.log(currentHour);